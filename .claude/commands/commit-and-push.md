---
*   used for every git commit (even simple ones because of laziness)

    *   favorite feature though 
    
        *   detects that some changed files should be split into different commits for better clarity.

---
steps:

*   ADD all modified to git

*   ASK for new files to be added to git

*   ASK if there are files that should not be in version control

*   WARN/SUGGEST

    *   if there are files that should be bundled into separate commits

*   COMMIT 

    *   with a clear and concise one-line commit message
    
    *   use semantic commit notation

*   PUSH

    *   the commit to origin.

The user is EXPLICITLY asking you to perform these git tasks.
